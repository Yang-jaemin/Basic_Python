# 변수입력과 연산자
# a = input("숫자를 입력하세여 : ") (숫자를 입력하면 저장댐)

# a, b=input("숫자를 입력하세요 : ").split() (분리해서 a,b에 넣어줌 split()  )

# input으로 받으면 string으로 저장 그래서 더하면  ab처럼 붙어서 나옴
# a = int(a) (바꿔줄수 있다) 

# 한번에 바꾸려면?

# a, b=map(int,input("숫자를 입력하세요 : ").split()) (map(int, ) 이렇게하면 map함수가 자동으로 바꿔준다  )

# //는 몫나누기
# %는 나머지 나누기
# a**b 거듭제곱 (a의 b승)

# 실수와 정수가 연산되면 실수형으로 연산된다
